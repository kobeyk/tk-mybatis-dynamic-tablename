<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bluethink.mapper.GxFeatureMapper">

    <resultMap type="cn.bluethink.entity.GxFeatureEntity" id="featureEntities">
        <id property="id" column="id"/>
        <result property="geom" column="geom"/>
        <result property="metaId" column="meta_id"/>
        <result property="attributes" column="attributes"/>
    </resultMap>

    <sql id="TABLE_COLUMNS">
        id,
        geom,
        meta_id,
        attributes
	</sql>

    <select id="getFeatures" statementType="PREPARED" resultType="java.util.List" resultMap="featureEntities">
        select <include refid="TABLE_COLUMNS"/>  from ${tableName}
    </select>


    <update id="createFeature" statementType="PREPARED" parameterType="string">
        CREATE TABLE  ${tableName} (
        "id" int8 NOT NULL,
        "geom" "public"."geometry",
        "meta_id" int8,
        "attributes" jsonb
          );
        CREATE INDEX '"' || 'INDEX_FEATURE_GEOM' || '_' || ${indexId} || '"' ON ${tableName} USING gist (
          "geom" "public"."gist_geometry_ops_2d"
        );
        CREATE INDEX  '"' || 'INDEX_FEATURE_METAID' || '_' || ${indexId} || '"'
           ON ${tableName} USING btree (
          "meta_id" "pg_catalog"."int8_ops" ASC NULLS LAST
        );
        ALTER TABLE ${tableName} ADD CONSTRAINT "gx_feature_pkey" PRIMARY KEY ("id");
    </update>

</mapper>